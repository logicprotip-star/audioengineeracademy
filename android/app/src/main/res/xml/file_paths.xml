<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <external-path name="my_images" path="." />
    <cache-path name="my_cache_images" path="." />
    <!-- G107: Capacitor Filesystem'in Directory.Data'sı Android'de context.filesDir'e
         eşleniyor (bkz. @capacitor/filesystem LegacyFilesystemImplementation.kt:54) —
         bu kök daha önce burada tanımlı DEĞİLDİ. @capawesome/capacitor-file-picker'ın
         copyFile() metodu hedef URI'yi FileProvider.getUriForFile() ile çözüyor;
         kapsayan bir <files-path> girdisi olmadan Directory.Data'ya kopyalama
         "Failed to find configured root" hatasıyla BAŞARISIZ olurdu. -->
    <files-path name="my_files" path="." />
</paths>